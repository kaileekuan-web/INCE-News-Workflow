# Core dependencies for the WAT framework

# Web framework
flask>=3.0.0
gunicorn>=21.2.0

# HTTP and web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
selenium>=4.15.0
undetected-chromedriver>=3.5.0

# Data processing
pandas>=2.1.0
openpyxl>=3.1.0

# Google APIs
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0
gspread>=5.11.0

# Environment management
python-dotenv>=1.0.0

# AI/LLM APIs
anthropic>=0.116.0                 # Claude — every inference in the pipeline
openai>=1.12.0                     # legacy: translate_content.py + --provider openai

# News collection
newsapi-python>=0.2.7              # NewsAPI.org client
feedparser>=6.0.10                 # RSS feed parsing

# Word document generation
python-docx>=1.1.0                 # DOCX creation

# Utilities
pytz>=2023.3                       # Timezone handling
python-dateutil>=2.8.2             # Date parsing

# NOTE: the claim-envelope package is deliberately NOT listed here.
#
# It is an editable install from a sibling checkout (-e ../INCE-Verification-Service/envelope),
# which cannot resolve inside the Docker build context -- pip fails outright and
# takes the whole image build with it. Keeping it here broke every container
# build. It lives in requirements-local.txt instead; install that for local work:
#
#     pip install -r requirements-local.txt
#
# tools/verify_emit.py degrades gracefully when the package is absent (logs a
# warning, submits nothing), which is the correct behaviour in a container: the
# verification service is reachable from a developer machine, not from Railway.
